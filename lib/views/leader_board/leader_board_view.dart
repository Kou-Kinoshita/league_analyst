import 'package:flutter/material.dart';

class leaderBoard extends StatelessWidget {
  const leaderBoard({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Text('playerAnalysis WIP'),
      ],
    );
  }
}